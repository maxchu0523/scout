/**
 * Single source of truth for Scout's version — used by the CLI, the MCP
 * server identity, and the MCP client handshake. Keep in sync with
 * package.json when bumping.
 */
export const VERSION = "0.3.0";
